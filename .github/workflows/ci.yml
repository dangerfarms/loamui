name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm -r build
        env:
          PAGES: "true"
      - run: pnpm -r lint
      - run: pnpm lint:md
      - run: pnpm format:check
      - run: pnpm --filter @farmui/core exec tsc --noEmit
      - run: npx tsc --noEmit
        working-directory: apps/docs
      - run: pnpm --filter @farmui/core audit:contrast
      - run: pnpm --filter @farmui/core test
